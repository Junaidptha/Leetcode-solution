class Solution {
    public boolean uniformArray(int[] nums1) {
        // Always possible: if an odd number exists, use it to turn every even number odd;
        // otherwise, all elements are already even.
        return true; 
    }
}